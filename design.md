# 考研错题本：数学一 × 可配置专业课

版本：2026-09-05
状态：当前唯一产品与开发基线

## 0. 产品结论

本项目不是四科通用学习平台，也不是先建设知识图谱或 RAG 平台。第一目标是把真实错题闭环做可靠：

```text
纸质题拍照，一题多图上传
→ 原图立即保存
→ 读取题面、参考答案和本人过程
→ 从资料形成可编辑的课程/章节/个人知识点候选
→ 诊断知识点不会、方法选择错误或推导/计算错误
→ 指出首次错误步骤
→ 用户确认后进入错题本
→ 回测时先选会/不会/不确定
→ 可选上传新的完整解题过程
→ 从已有题库按知识点、题型、难度挑选相似题
```

用户确认后的内容才是个人学习事实。模型、资料检索和题库只能提出候选或证据，不能绕过确认写入权威错题字段。

## 1. 课程范围

### 1.1 第一主线

第一版只把以下课程做深：

- **数学一**：固定课程，支持图片错题、诊断、个人知识树、回测和相似题筛选。
- **可配置专业课**：支持 408，以及电子类考研的“信号与系统 + 其他自定义课程组合”。专业课不能写死成信号与系统，也不能把所有专业课压成一个 `professional` 知识空间。

课程是可创建和可命名的实体：

```text
课程组：408 / 电子类考研 / 其他
课程：数学一 / 数据结构 / 操作系统 / 信号与系统 / 自定义课程
章节：属于一门课程
知识树：每门课程独立
题目、资料、题库：必须关联到课程
```

408 可以先作为一个课程使用，也可以后续拆成四门子课程；不提前建设复杂课程管理系统。

### 1.2 第二阶段

政治作为独立的章节选择题库接入：

```text
导入题库文件
→ 按章节刷选择题
→ 对照标准答案
→ 查看解析
→ 收集错题
→ 按章节重复练习
```

政治第一阶段只做客观选择题，不做图片错题主线、分析题和主观诊断。题库来源先支持用户导入，外部题库以后再评估。

### 1.3 明确排除

英语暂不开发，不进入第一版验收、首屏导航、知识树、资料召回或课程统计。

同时不做自动学习计划、模型最终主观判定、长资料自动总结、自动生成新题作为主路径、多用户、云同步、网络部署、独立图数据库、复杂知识图谱、多套 RAG、FSRS 和多端客户端。

## 2. 错题流程

### 2.1 收录

一次上传代表一道题，可以包含多张图片并保留顺序。角色为 `question`、`my_process`、`reference`、`mixed` 或回测用 `redo_process`。

图片先保存，角色可后改。分析失败、资料未命中或模型不可用都不删除原图，也不阻止继续编辑和确认。

### 2.2 候选生成

答案来源按以下优先级组织：

```text
用户上传的参考答案图
> 用户资料中的答案
> 已导入题库答案
> LLM 解答
```

来源冲突时并列显示并标记“待确认”，不能静默覆盖。模型输出始终是可编辑草稿。

候选字段包括题面、参考答案、章节、个人知识点、题型/方法、错误类型、首次错误步骤和正确思路。错误诊断只聚焦：

1. 知识点不会；
2. 方法选择错误；
3. 推导或计算出错。

无法确定时显示不确定，不强行猜测。最低结果是文字定位首次错误步骤、错误类型和正确分叉；图片区域标注以后再做。

### 2.3 确认

用户可以修改所有候选字段并确认。字段为空仍允许确认，正式错题显示“待补充”。继续编辑只更新当前正式版本，不改写历史 Attempt 快照。

正式错题最小字段：

```text
原题图片、本人原始解题过程、正确答案或参考解、
错误类型、首次出错步骤、关联知识点、关联章节、题型或方法
```

## 3. 个人知识树

知识点不是四门课共用的字符串。系统从每门课程的教材、讲义、笔记和题库中自然聚类候选：

```text
资料/题库
→ 知识点名称、别名、上下级关系候选
→ 用户可在确认前修正名称和上级章节
→ 用户确认
→ 课程自己的个人知识树
→ 错题、资料和相似题回链
```

第一阶段只要树形节点和别名，不做图数据库。系统可以建议同义词合并，但最终由用户确认；不同课程中同名概念默认不自动合并。

建议逻辑对象：`Course`、`KnowledgeNode`、`SourceArtifact/SourcePassage`、`Question`、`QuestionBankItem`、`QuestionKnowledgeLink`。

## 4. 回测

固定 +3/+7/+10/+14 天作为第一版默认排程。到期回测先分流：

```text
只显示题面
→ 选择：会 / 不会 / 不确定
→ 三种状态都可以手动改成完整重做
→ 完整重做时上传新的过程图片
→ 保存新的 Attempt
→ 可选模型比较和诊断草稿
```

默认行为：

- **会**：记录自评，可直接结束，也可完整重做；
- **不会**：默认进入完整重做；
- **不确定**：保留状态，由用户决定是否完整重做；
- **完整重做**：允许上传一张或多张 `redo_process` 图片。

系统不根据一次“会”自动宣布掌握，不自动覆盖原错误诊断，不因模型比较失败回滚 Attempt。FSRS 后置。

## 5. 相似题与题库

“举一反三”的第一实现是从已有题库找真实题目，不是生成新题：

```text
当前错题的课程、章节、知识点、题型、难度
→ 查询已导入题库
→ 返回候选
→ 用户选择练习
```

错题详情提供知识点、章节、题型和难度筛选入口；筛选条件只在当前错题所属课程内生效，空条件时仍按当前错题元数据做匹配排序。

题库最小字段：`course_id`、题面/图片、章节、`knowledge_node_id`、题型、难度、参考答案、解析、来源/年份。

第一版使用 FTS/LIKE 和元数据筛选即可。语义相似召回、自动生成新题和外部题库服务后置。

## 6. 资料和检索

原始资料先保存，再增强。可以继续支持文字 PDF、扫描 PDF、DOCX、图片和普通静态网页；解析失败只改变状态，不删除原件。

检索只负责返回 passage、出处和候选证据，不直接写入正式错题事实。题目分析、匹配和同题追问带有课程身份时，只召回当前课程资料和未分类旧资料，避免 408/信号与系统等专业课之间串入答案；FTS 可用时加速，不可用时继续 LIKE；不因向量或图能力未接入阻塞原始输入。

## 7. 当前实现对照

### 已有且保留

- SQLite + `objects/` 的本地事实和原始资产；
- 一题多图 intake、图片角色、顺序和追加；
- 多协议视觉模型草稿和回退；
- 资料解析、SourcePassage、FTS/LIKE 和出处回链；
- 确认后生成 Question、Attempt 和 ReviewTask；
- 历史 Attempt 快照隔离；
- 正式错题按课程/科目/章节/知识点/题型/错误类型筛选和导出，列表及知识导航直接显示章节、知识点、错误类型、错误原因和首次出错步骤，支持同题追问；
- 知识导航支持按课程切换，候选节点和已确认题目按同一课程范围显示，避免多门专业课在界面上混树；
- 正式错题列表同时显示答案来源和少量字段来源摘要，用户可以在打开详情前优先扫描待复核内容；
- 错题详情展示初次作答和已提交回测 Attempt 历史及过程图片；闭卷回测期间仍隔离历史作答、参考答案和比较诊断；
- 分析草稿中的字段来源会随确认写入正式 QuestionRevision，错题详情和 Markdown 导出继续标出模型、资料、题库或用户修改来源；确认后的来源不会被后续普通字段编辑静默抹掉；
- 图片分析解析兼容编号/Markdown 标题、结构化 JSON 及常见答案、方法和错误分析标签变体；解析失败时保留原始分析文本和原图，字段仍需用户确认；
- 固定回测和 macOS 到期通知；
- 模型、解析和比较失败不阻断保存。

### 已按本基线完成的收口

- `Course` 已成为题目、资料和题库的课程身份；默认提供数学一、408、信号与系统，并允许创建自定义专业课；
- `KnowledgeNode` 和 `QuestionKnowledgeLink` 已提供课程独立的个人知识树；解析出的章节/知识点可以先落为候选节点，用户选择后升级为确认节点，题目再绑定同课程节点；正式错题页也可以重新绑定或解除同课程知识节点，历史 Attempt 不变；
- 已确认知识节点改名会同步当前 QuestionRevision 和对应 intake 草稿中的知识点文本，历史 Attempt 仍保留当时的快照；
- 带课程资料增强会从明确章节标题生成 `source_heading` 候选；首页知识导航支持逐个确认或忽略，候选不会直接成为正式学习事实；
- 题库已支持 JSON API 和 CSV/JSON 文件导入，按课程、章节、知识点、题型和难度筛选，并从错题详情返回真实题库候选；
- 题库导入已提供逐行预览和字段级错误反馈，当前文件内重复题目 ID 会在预览阶段报错，只有校验通过的当前文件才允许写入；
- 题库维护已支持按课程逐题编辑和 JSON 批量更新，课程身份不可在维护中漂移；
- 政治已拥有独立默认课程和客观题刷题入口，支持选项、标准答案、解析、章节筛选、自动判断对错和错答记录；错答记录保留选项并支持直接再做一次；政治记录不进入数学/专业课图片错题主链；
- 回测已支持会/不会/不确定/完整重做分流，状态写入独立的 session action；
- 错误诊断已提供三项受控类型：知识点不会、方法选择错误、推导或计算出错；仍保留错误原因和首次错误步骤的自由文本补充；
- 首页课程选择、题库导入和相似题入口已接通，旧 `subject_key` 仍只承担兼容投影；首页已移除学习目标状态、证据变化和统计面板，只保留回测队列与错题主链入口。

### 当前能力限制与下一步

- P0–P5 的“第一版完成”表示代码流程已接通，不代表真实手写照片上的题面、公式、参考答案提取和首次错误定位已达到日常使用质量。下一轮先沿第 10 节完整流程验证，按实际失败修复；未验证的模型效果保持待验证。
- 个人知识树当前从明确章节、编号小节和“知识点/考点”行生成最多两级候选，只做同课程空格、标点等标签变体的保守归并并保留别名。跨资料语义聚类仍未实现；下一阶段继续围绕资料候选的整理和确认推进。
- 相似题当前依赖已有题库、元数据和 FTS/LIKE。答案候选能并列确认并保留出处，但资料中的答案仍需明确段落或用户修正；检索命中不能视为答案正确。

### 当前不应继续扩张

LightRAG、向量库、复杂版面旁路、FSRS、英语、政治图片错题、政治分析题、自动生成新题、整体学习计划和大规模统计。

## 8. 调整后的执行路线

### P0：基线与课程身份（已完成）

先更新文档和入口，明确数学一、408、自定义专业课是第一主线。代码增加轻量 `Course`/课程配置能力，让资料、题目和题库关联具体课程；保留旧 `subject_key` 作为兼容投影，不再把它当长期领域模型。

验收：可以创建数学一、408、信号与系统和一门自定义课程，并在上传题目和资料时选择课程。

### P1：个人知识树和候选来源（第一版已完成）

增加课程独立的 `KnowledgeNode` 和题目/资料关系。资料解析或错题分析产生的章节/知识点先保存为候选，用户选择并确认后再升级为正式节点；不要求一次性建立完美树。

验收：同一课程下可以确认知识点树，题目能挂到节点，节点能反查题目和资料；不同课程不会意外混合。

### P2：错题收录主链收口（第一版已完成）

沿用现有 intake，补齐答案来源优先级、来源徽标、冲突候选和三类诊断提示。保持 write-first 和字段可空；资料答案目前以段落候选形式呈现，用户确认后才进入正式字段。

验收：一题多图上传后能看到题面、答案、分类、错误诊断及来源，用户确认后才进入正式错题。

### P3：回测分流（已完成第一版）

在现有 ReviewSession/Attempt 上增加“会/不会/不确定/完整重做”状态和手动升级完整重做路径。固定间隔暂不改。

验收：会可以结束，不会默认重做，不确定可稍后决定，三者都能手动上传完整过程；历史 Attempt 不被覆盖。

### P4：题库导入与相似题筛选（已完成第一版）

先支持用户导入 CSV/JSON 题库，先预览并逐行校验，再按课程、章节、知识点、题型和难度筛选；暂不接外部题库服务，暂不生成新题。

验收：从一条数学或专业课错题能挑出真实题库中的相似题，点击“开始练习”生成待确认草稿，并在用户确认后进入正式错题本。

### P5：政治选择题库（第一版已完成）

建立独立题库导入和章节刷题流，支持标准答案、解析、客观判断和错答记录。政治不反向改变数学/专业课图片主链。

### 后置评估

只有出现可记录的真实失败后才评估语义召回、复杂版面解析、LightRAG 或 FSRS。每次评估必须记录触发证据、首选方案、事实边界和退出条件。

## 9. 开发纪律和边界

- SQLite 是唯一学习事实源，`objects/` 保存原件；派生文本、索引和知识候选可重建。
- 模型、资料和题库只提供候选或证据；用户确认后的字段才是正式事实。
- 不因模型、OCR、解析、检索或比较失败阻塞原始输入。
- 新功能必须服务数学/专业课主闭环或政治题库，不能仅因技术可用加入。
- 先改当前真实用户路径，再补最小定向验证。
- 一轮交付覆盖一条完整用户流程及其直接问题，相关前后端改动统一验证；已有检查通过且没有新证据时继续开发，不重复审查同一结论。阶段状态在本文件更新，具体提交和测试日志不进入长期记忆。
- 旧文字 P0 演示链保留作兼容验证，但不再定义产品方向。
- 不覆盖或删除已有数据库和原始图片；结构变化采用可追踪的最小迁移。

## 10. 第一版验收场景

```text
创建数学一或自定义专业课
→ 上传一题的多张纸质题照片
→ 原图按顺序保存
→ 自动识别题面
→ 从参考答案、资料、题库或 LLM 生成答案候选
→ 生成章节、知识点、题型和首次错误诊断候选
→ 用户确认和修改
→ 进入正式错题本
→ 到期选择会/不会/不确定
→ 可选上传完整新解题过程
→ 从已导入题库按知识点/题型/难度挑选相似题
```

这条链稳定之前，不把项目宣传成完整四科考研平台，也不扩张到英语、自动计划、自动主观判卷或复杂 RAG。
