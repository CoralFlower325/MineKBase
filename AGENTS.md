# AGENTS.md

## 项目定位

这是一个单用户、本地优先的数学一与可配置专业课图片错题本，另有独立的政治章节选择题库。
目标架构和当前阶段以 `design.md` 为准，接口和启动方式以 `README.md` 为准。

## 开发前

- 先阅读 `README.md` 和 `design.md`，再开始修改。
- 保持当前架构边界：单体 HTTP 应用、SQLite 唯一事实源、`objects/` 保存原始资产。
- 不要因为增强失败、字段缺失或模型不可用而阻止原始输入保存。

## 协作规则

- `main` 只接受 Pull Request，不直接在 `main` 上开发。
- 除 `main` 外始终只保留一个现役开发分支；当前唯一现役分支 `mzy` 是用户的个人分支，当前尚无协作者分支。未经用户明确要求，不创建并行功能分支或恢复旧分支；若以后明确启用协作者分支，各自使用独立工作目录并通过 Pull Request 汇入 `main`，不把 `mzy` 当共享分支。
- 一个任务尽量由一个 Codex 负责；不要让两个 Codex 同时改同一批文件。
- 开始新一轮前在 `mzy` 上同步 `git pull --ff-only origin mzy`；准备进入 `main` 前先核查当前 diff 并运行相关定向验证，通过 Pull Request 合并，不直接推送 `main`。协作者分支按同样规则分别核查后提交 PR。
- 提交保持小而明确，提交信息使用 `feat:`、`fix:`、`refactor:`、`docs:` 等前缀。
- 修改与当前任务无关的文件前，先在 Pull Request 中说明原因。

## 数据与敏感信息

- 不提交 API key、密码、个人配置或本地环境文件。
- `library.sqlite` 和 `objects/` 是本地运行数据，默认不提交；需要共享的演示数据放在 `fixtures/`。
- 不覆盖或删除用户已有的本地数据库和原始图片。

## 验证

- 修改后优先验证当前真实切片，不要求每次运行全量测试矩阵。
- 按一条完整用户流程组织一批相关改动并统一验证；通过后仅在新改动、失败或未决问题出现时追加检查，不把单个字段或文案反复拆成开发轮次。
- Python 修改至少确认语法可编译；涉及现有流程时运行 README 中对应的定向命令。
- 在提交前检查 `git diff` 和 `git status`，确认没有把本地数据或秘密文件带入提交。

## Codex 工作提示

开始任务时先说明：当前分支、目标、允许修改的范围和验证命令。
完成后报告：修改文件、行为变化、已运行的验证，以及尚未验证的部分。

## 项目记忆

- mmemory 使用 `minekbase` 项目键，任务开始时召回一次；本轮未出现新的决策问题时不重复召回。
- 最多保留架构入口、阶段里程碑、用户偏好各一条；产品与路线只在 `design.md` 解释，使用命令在 `README.md`，逐次改动和验证结果留在 Git 与任务记录。
- 只有基线、明确偏好或阶段验收状态变化才更新记忆；小功能进度不续写里程碑，旧结论归档，不增加平行版本。
